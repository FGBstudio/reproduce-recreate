# ============================================================
# FGB MQTT Ingestion Service Configuration
# ============================================================

# Supabase Configuration (REQUIRED)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# MQTT Configuration
MQTT_BROKER_URL=mqtt://mosquitto:1883
MQTT_USERNAME=
MQTT_PASSWORD=

# Topics to subscribe (comma-separated)
# Supports wildcards: # (multi-level), + (single-level)
# Excludes $SYS/# automatically
MQTT_TOPICS=fosensor/#,bridge/#,MSCHN/#

# Default site ID for auto-registering new devices (optional)
# If not set, devices must be pre-registered in the database
DEFAULT_SITE_ID=

# Ingestion Settings
BATCH_SIZE=100
BATCH_INTERVAL_MS=5000

# Retry Configuration
MAX_RETRIES=5
RETRY_BASE_DELAY_MS=1000

# Logging
LOG_LEVEL=info

# Health Server
PORT=3001

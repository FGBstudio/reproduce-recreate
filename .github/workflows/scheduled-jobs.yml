name: Scheduled Aggregation Jobs

# Schedule DISABLED on purpose (2026-09-05): pg_cron inside the database is
# the single clock now (job 1 hourly at :05, job 2 daily at 01:30, purge at
# :45). This workflow used to fire at the very same minute and the two
# collided on the same upserts, failing every scheduled run. Keep it for
# MANUAL catch-up runs only (workflow_dispatch below).
on:
  workflow_dispatch:
    inputs:
      job_type:
        description: 'Job type to run'
        required: true
        default: 'hourly'
        type: choice
        options:
          - hourly
          - daily
          - all
          - power
          - purge

jobs:
  run-aggregation:
    runs-on: ubuntu-latest
    steps:
      - name: Determine job type
        id: job
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "type=${{ github.event.inputs.job_type }}" >> $GITHUB_OUTPUT
          elif [ "${{ github.event.schedule }}" = "15 1 * * *" ]; then
            echo "type=daily" >> $GITHUB_OUTPUT
          else
            echo "type=hourly" >> $GITHUB_OUTPUT
          fi
      
      - name: Run aggregation job
        run: |
          echo "Running ${{ steps.job.outputs.type }} job..."
          
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/scheduled-jobs?job=${{ steps.job.outputs.type }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json")
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)
          
          echo "Response (HTTP $HTTP_CODE):"
          echo "$BODY" | jq .
          
          if [ "$HTTP_CODE" -ge 400 ]; then
            echo "Job failed with HTTP $HTTP_CODE"
            exit 1
          fi
          
          # Check if all jobs succeeded
          SUCCESS=$(echo "$BODY" | jq -r '.success')
          if [ "$SUCCESS" != "true" ]; then
            echo "Some jobs failed"
            exit 1
          fi
          
          echo "✓ Job completed successfully"
